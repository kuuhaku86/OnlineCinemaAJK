"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _os = _interopRequireDefault(require("os"));

var _fs = _interopRequireDefault(require("fs"));

var _path = _interopRequireDefault(require("path"));

var _nanoid = _interopRequireDefault(require("nanoid"));

var _File = _interopRequireDefault(require("../File"));

var _getFieldPath = _interopRequireDefault(require("../util/getFieldPath"));

var _FileSizeLimitError = _interopRequireDefault(require("../error/FileSizeLimitError"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Get a file from part and push it to entries array
 *
 * @api private
 */
const onFile = ({
  limits
}, cb) => (fieldname, stream, filename, enc, mime) => {
  try {
    const fieldPath = (0, _getFieldPath.default)(fieldname);
    filename = _path.default.join(_os.default.tmpdir(), `${(0, _nanoid.default)()}__${filename}`);

    function onEnd() {
      const contents = _fs.default.createReadStream(filename);

      const file = new _File.default({
        filename,
        contents,
        enc,
        mime
      });
      cb(null, [fieldPath, file]);
    }

    const onLimit = () => cb(new _FileSizeLimitError.default(`Limit reached: Available up to ${limits.fileSize} bytes per file.`));

    stream.once("error", cb).once("end", onEnd).once("limit", onLimit).pipe(_fs.default.createWriteStream(filename));
  } catch (err) {
    return cb(err);
  }
};

var _default = onFile;
exports.default = _default;
import _includesInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/includes";
import _sliceInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/slice";
const basicTypes = ["null", "number", "object", "array", "string", "function", "undefined", "boolean"];
/**
 * Get a string with type name of the given value
 *
 * @param {any} val
 *
 * @return {string}
 *
 * @api private
 */

function getType(val) {
  var _context;

  const type = _sliceInstanceProperty(_context = Object.prototype.toString.call(val)).call(_context, 8, -1);

  if (_includesInstanceProperty(basicTypes).call(basicTypes, type.toLowerCase())) {
    return type.toLowerCase();
  }

  return type;
}

export default getType;
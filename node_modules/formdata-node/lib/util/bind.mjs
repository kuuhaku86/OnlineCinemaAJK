import _bindInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/bind";

/**
 * @api private
 */
function bind(names, ctx) {
  for (const name of names) {
    const fn = ctx[name];
    ctx[name] = _bindInstanceProperty(fn).call(fn, ctx);
  }
}

export default bind;